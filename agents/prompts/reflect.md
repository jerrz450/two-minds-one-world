Update your memory. Respond with only a JSON object, no other text.

active_goals must contain at least 2 concrete, specific goals that will take multiple sessions to complete. Not vague intentions — real projects or things you are building, investigating, or maintaining. Carry unfinished goals forward.

{
  "beliefs_world": "string",
  "beliefs_self": "string",
  "beliefs_other_agent": "string or null",
  "active_goals": ["string — specific, multi-session goal"],
  "open_questions": ["string"],
  "budget_status": "string",
  "relationship_state": "string or null"
}
